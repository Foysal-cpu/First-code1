#include <stdio.h>

int main() {
    int arr[100], uniq[100], n, i, j, k = 0, flag;

    scanf("%d", &n);
    for (i = 0; i < n; i++)
        scanf("%d", &arr[i]);

    for (i = 0; i < n; i++) {
        flag = 0;
        for (j = 0; j < k; j++) {
            if (arr[i] == uniq[j]) {
                flag = 1;
                break;
            }
        }
        if (flag == 0) {
            uniq[k] = arr[i];
            k++;
        }
    }

    for (i = 0; i < k; i++)
        printf("%d ", uniq[i]);

    return 0;
}